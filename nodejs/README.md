# Node.js

## Overview

This folder has the code that demonstrates various features of [Node.js](https://nodejs.org/).

## How to use

- Run `yarn install` to install the dependent modules.
- Run `node <filename.js>` or `./<filename>.js` to run the code.
